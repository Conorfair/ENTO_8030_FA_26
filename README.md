# ENTO 8030 – Applied Statistics for Graduate Students

**Fall 2026**

Welcome to the course GitHub repository for ENTO 8030.

This repository contains datasets, R scripts, discussion materials, and supporting resources that we will use throughout the semester.

A major goal of this course is not only to learn statistical methods, but also to develop an organized and reproducible approach to statistical analysis. We will therefore use **GitHub and R Projects throughout the semester** to organize and access course materials.

---

## Getting Started

Before the first discussion session, you should have the following installed:

* **R**
* **RStudio**
* **Git**
* A **GitHub account**

During Discussion 01, we will connect these tools and practice using them together.

---

## Git, GitHub, R, and RStudio

These tools have related but different purposes.

### R

R is the programming language that performs our statistical analyses.

### RStudio

RStudio is the interface we will use to write, organize, and run R code.

### R Project

An R Project defines the working directory associated with an analysis or project.

Throughout ENTO 8030, we will use R Projects so that analyses can use **relative file paths** rather than paths tied to one particular computer.

### Git

Git tracks changes to files over time.

### GitHub

GitHub stores and shares Git repositories online.

A simplified workflow is:

```text
GitHub Repository
       |
       | clone / pull
       v
Local R Project
       |
       | edit files and run analyses
       v
Git Commit
       |
       | push
       v
GitHub Repository
```

---

# Accessing the Course Repository

The ENTO 8030 course repository is:

**ENTO_8030_FA_26**

You will create a local copy of this repository on your computer.

## Step 1 – Copy the Repository URL

On the GitHub repository page:

1. Click the green **Code** button.
2. Select **HTTPS**.
3. Copy the repository URL.

It should look similar to:

```text
https://github.com/Conorfair/ENTO_8030_FA_26.git
```

---

## Step 2 – Clone the Repository Using RStudio

Open RStudio.

Select:

**File → New Project → Version Control → Git**

Paste the repository URL into the **Repository URL** field.

Choose a location on your computer where you want the course repository stored.

Then select:

**Create Project**

RStudio will download the repository and open the course R Project.

---

## Step 3 – Confirm That the Project Is Open

In the R Console, run:

```r
getwd()
```

Your path will depend on where you saved the repository.

You can also run:

```r
list.files()
```

You should see files and folders similar to:

```text
data
discussions
ENTO 8030 Repo.Rproj
output
README.md
resources
```

If you see these files, you are working inside the ENTO 8030 R Project.

---

# Repository Organization

The repository is organized into several folders.

```text
ENTO 8030 Repo/
│
├── data/
│
├── discussions/
│
├── output/
│   ├── figures/
│   └── tables/
│
├── resources/
│
├── ENTO 8030 Repo.Rproj
│
└── README.md
```

### `data/`

Contains datasets used during course discussions and exercises.

### `discussions/`

Contains R scripts and other materials for discussion sessions.

Discussion materials are organized by topic.

For example:

```text
discussions/
└── 01_github_rprojects/
    └── Discussion 01.R
```

### `output/`

Provides a location for figures, tables, and other outputs generated by R.

### `resources/`

Contains reference materials that may be useful throughout the semester, including guidance for using R and troubleshooting common problems.

### `README.md`

This file provides instructions for accessing and navigating the course repository.

---

# Discussion 01

## GitHub, R Projects, and Getting Help in R

For the first discussion session, open:

```text
discussions/01_github_rprojects/Discussion 01.R
```

During this discussion, we will work through the script together.

Topics include:

* Git and GitHub
* R Projects
* Working directories
* Relative file paths
* Importing data
* R objects
* Variables
* Functions
* Function arguments
* R help pages
* Package documentation
* Creating and saving output
* Pulling, committing, and pushing with Git

---

# An Important Course Practice: Relative File Paths

Throughout ENTO 8030, we will avoid code that depends on the location of files on one particular computer.

For example, avoid code such as:

```r
setwd("/Users/YourName/Desktop/Research/")
```

or:

```r
dat <- read.csv(
  "C:/Users/YourName/Documents/Research/data/mydata.csv"
)
```

These paths may work on your computer but will usually fail on someone else's computer.

Instead, open the appropriate R Project and use paths relative to the project directory:

```r
dat <- read.csv("data/mydata.csv")
```

The same code can then work regardless of where the project is stored.

---

# Updating Your Course Repository

New materials may be added to the GitHub repository during the semester.

Before beginning a discussion session, make sure your local repository contains the most recent course materials.

In RStudio, this can be done using **Pull** from the Git pane.

Conceptually:

```text
GitHub
   |
   | PULL
   v
Your Local Course Repository
```

We will practice this workflow during Discussion 01.

---

# Git Vocabulary

Three terms will be especially useful throughout the semester.

## Pull

Bring changes from the GitHub repository onto your computer.

## Commit

Create a local checkpoint recording changes to files.

A useful commit message briefly explains what changed.

For example:

```text
Complete Discussion 01 exercises
```

## Push

Send your local commits to GitHub.

The basic workflow is:

```text
PULL
  ↓
Edit files
  ↓
COMMIT
  ↓
PUSH
```

---

# Getting Help in R

You are **not expected to memorize every R function or command**.

Learning how to find reliable information is an important part of learning R.

For example:

```r
?mean
```

opens the help page for `mean()`.

You can also use:

```r
help(mean)
args(mean)
example(mean)
```

R help pages commonly contain sections such as:

* Description
* Usage
* Arguments
* Details
* Value
* Examples

We will practice reading and using R documentation during Discussion 01.

---

# Course Philosophy

Statistical analysis involves more than choosing a statistical test.

Throughout ENTO 8030, we will develop a workflow that connects:

```text
Scientific Question
        ↓
Data
        ↓
Data Exploration
        ↓
Statistical Model
        ↓
Model Evaluation
        ↓
Hypothesis Testing and Estimation
        ↓
Figures and Tables
        ↓
Scientific Interpretation
```

The organization of this repository is designed to support that workflow throughout the semester.
